apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chaos-platform.fullname" . }}-config
  labels:
    {{- include "chaos-platform.labels" . | nindent 4 }}
data:
  config.yaml: |
    server:
      port: 8000
